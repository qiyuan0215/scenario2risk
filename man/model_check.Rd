% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_check.R
\name{model_check}
\alias{model_check}
\title{Check VaR exceedance rates}
\usage{
model_check(
  macro_data,
  return_data,
  portfolio,
  horizon = 12,
  n_scenarios = 300,
  k = 2,
  var_lag = 1,
  seed = 123,
  window = 120,
  n_origins = 36,
  use_parallel = FALSE,
  n_cores = 2
)
}
\arguments{
\item{macro_data}{A data frame that contains \code{date} and macro predictor
columns.}

\item{return_data}{A data frame with \code{date}, \code{equity_return}, \code{bond_return},
and \code{cash_return} columns.}

\item{portfolio}{A data frame that contains weights for the required
assets \code{equity}, \code{bond}, and \code{cash}.}

\item{horizon}{Integer VaR horizon in months.}

\item{n_scenarios}{Integer number of simulated FAVAR paths at each rolling
forecast origin.}

\item{k}{Integer number of principal-component macro factors used in the
FAVAR state vector.}

\item{var_lag}{Integer lag order for the VAR fitted to macro factors and
asset returns.}

\item{seed}{Integer random seed used for bootstrap simulation.}

\item{window}{Integer rolling estimation window in months.}

\item{n_origins}{Integer number of most recent forecast origins to check.}

\item{use_parallel}{Logical. If \code{TRUE}, rolling origins are evaluated with a
PSOCK cluster; otherwise they are evaluated sequentially.}

\item{n_cores}{Integer number of worker processes used when
\code{use_parallel = TRUE}.}
}
\value{
An object of class \code{model_check}, a list with:
\code{var_exceedance}, a summary table comparing FAVAR scenario VaR and
Historical Simulation VaR exceedance rates.
}
\description{
\code{model_check()} performs a rolling, horizon-aligned VaR exceedance check.
At each historical forecast origin, it fits the FAVAR model on the previous
\code{window} months, simulates h-horizon portfolio losses, and compares the
resulting 95 percent VaR with the realized h-horizon portfolio loss. It also
reports the same exceedance rate for a Historical Simulation VaR benchmark.
}
\examples{
check <- model_check(
  macro_data = demo_macro_data,
  return_data = demo_return_data,
  portfolio = demo_portfolio_weights,
  horizon = 12,
  n_scenarios = 200,
  k = 2,
  var_lag = 1,
  seed = 123,
  window = 120,
  n_origins = 24,
  use_parallel = FALSE
)
}
