% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/portfolio_risk.R
\name{portfolio_risk}
\alias{portfolio_risk}
\title{Estimate portfolio downside risk under simulated macro-financial scenarios}
\usage{
portfolio_risk(
  macro_data,
  return_data,
  portfolio,
  horizon = 60,
  n_scenarios = 1000,
  k = 2,
  var_lag = 1,
  seed = 123
)
}
\arguments{
\item{macro_data}{A data frame that contains \code{date} and macro predictor
columns.}

\item{return_data}{A data frame with \code{date}, \code{equity_return}, \code{bond_return},
and \code{cash_return} columns.}

\item{portfolio}{A data frame that contains weights for the required
assets \code{equity}, \code{bond}, and \code{cash}.}

\item{horizon}{Integer forecast horizon in months.}

\item{n_scenarios}{Integer number of simulated future paths.}

\item{k}{Integer number of principal-component macro factors used in the
FAVAR state vector.}

\item{var_lag}{Integer lag order for the VAR fitted to macro factors and
asset returns.}

\item{seed}{Integer random seed used for bootstrap simulation.}
}
\value{
An object of class \code{portfolio_risk}, a list with:
\code{risk_summary}, a one-row table of mean loss, median loss, probability of
loss, 95 percent VaR, and 95 percent expected shortfall;
\code{loss_distribution}, one terminal loss per simulated scenario.
}
\description{
\code{portfolio_risk()} fits a factor-augmented VAR model to monthly macro
predictors and equity/bond/cash returns, simulates future return paths, and
reports terminal portfolio loss risk over the requested horizon.
}
\examples{
risk <- portfolio_risk(
  macro_data = demo_macro_data,
  return_data = demo_return_data,
  portfolio = demo_portfolio_weights,
  horizon = 60,
  n_scenarios = 500,
  k = 2,
  var_lag = 1,
  seed = 123
)
}
